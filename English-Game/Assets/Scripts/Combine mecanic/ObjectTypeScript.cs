using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectTypeScript : MonoBehaviour
{
    //Only gives the object an objectType (or in this case it's called BoxType)
    //Can be changed in unity itself with enums
    public BoxType boxColor;
}
